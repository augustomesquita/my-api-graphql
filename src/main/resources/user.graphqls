schema {
    query: Query
}

type Query {
    allUser: [User]
    user(id: Long): User
}

type User {
    id: Long
    name: String
    age: Int
    movie: Movie
}

type Movie {
    id: Long
    name: String
    director: String
}