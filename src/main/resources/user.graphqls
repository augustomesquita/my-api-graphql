type User {
    id: Long
    name: String
    age: Int
    movie: Movie
}

type Movie {
    id: Long
    name: String
    director: String
}

type Query {
    getAllUser: [User]
    getUser(id: Long): User
}