type Movie {
    id: ID!
    name: String!
    director: String!
}

# A Root Query da aplicação
type Query {
    getAllMovies: [Movie]
}

# A Root Mutation da aplicação
type Mutation {
    saveMovie(name: String!, director: String!): Movie
}